import '../styles/Card.css';
import Form from './Form';
import Text from './Text';

const Card = () => {
  return (
    <section className='card'>
      <Text />
      <Form />
    </section>
  );
};

export default Card;
